<%= simple_form_for [ @list, @bookmark] do |f| %>
    raise
    <%= f.input :comment %>
    <%= f.association :movie %>
    <%= f.submit "Add movie to the list", class: "btn btn-primary" %>
<% end %>
